# Local API port. Kept separate from other projects that may use 4000.
PORT=4001

# Keep the Turnstile secret on the API server only.
TURNSTILE_SECRET_KEY=your_turnstile_secret_key

# Optional extra response validation, for example: game.hexrivals.com
TURNSTILE_EXPECTED_HOSTNAME=
